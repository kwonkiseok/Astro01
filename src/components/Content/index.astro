---

const { pageTitle } = Astro.props;
---
<main class="main flex-1 p-6">
    { 
        ()=> {
          if (pageTitle != 'Home') {
            return( <h2>{ pageTitle }</h2> )
          }
        }
      }
    <slot />
</main>